// Configuración global de la aplicación
export const config = {
  // Establecer en false para deshabilitar la integración con Supabase
  useSupabase: false,

  // Otras configuraciones de la aplicación
  appName: "Unicuces",
  demoMode: true,
}
